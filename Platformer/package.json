{
  "name": "platformer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "transpile": "julia ../../JulGame.jl/tools/convert.jl --project-root . --all-scripts",
    "transpile:engine": "julia ../../JulGame.jl/tools/convert.jl",
    "build:wasm": "npm run build:wasm --prefix ../../JulGame.jl/ts"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
